# 🚀 Развертывание бота

## ❓ Будет ли работать бот если выключить ноутбук?

### **НЕТ, бот перестанет работать! ❌**

Бот работает как обычная программа на вашем компьютере. Когда вы выключаете ноутбук — бот останавливается.

---

## ✅ РЕШЕНИЯ: Как сделать бота доступным 24/7?

### 🎯 Вариант 1: VPS/VDS Сервер (РЕКОМЕНДУЕТСЯ)

**Что это?** Виртуальный сервер, который работает 24/7

#### Популярные провайдеры (Россия):

**🇷🇺 Российские:**
- **Selectel** - от 250₽/месяц
- **Timeweb** - от 169₽/месяц
- **Reg.ru** - от 150₽/месяц
- **Beget** - от 180₽/месяц

**🌍 Международные:**
- **DigitalOcean** - от $4/месяц
- **Linode** - от $5/месяц
- **Vultr** - от $3.50/месяц

#### Инструкция по развертыванию на VPS:

```bash
# 1. Подключитесь к серверу по SSH
ssh root@your-server-ip

# 2. Установите зависимости
apt update
apt install python3-pip git

# 3. Клонируйте проект
git clone <ваш-репозиторий>
cd bot

# 4. Установите зависимости Python
pip3 install -r requirements.txt

# 5. Создайте .env файл
nano .env
# Вставьте ваши данные и сохраните (Ctrl+X, Y, Enter)

# 6. Запустите бота в фоновом режиме
nohup python3 main.py > bot.log 2>&1 &

# Или используйте screen/tmux для управления сессией
screen -S bot
python3 main.py
# Ctrl+A, D для выхода (бот продолжит работать)
```

---

### 🎯 Вариант 2: Systemd Service (автозапуск на VPS)

Создайте сервис, который автоматически запустится при перезагрузке:

```bash
# Создайте файл сервиса
nano /etc/systemd/system/student-bot.service
```

```ini
[Unit]
Description=Student Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/bot
Environment="PATH=/usr/local/bin:/usr/bin:/bin"
ExecStart=/usr/bin/python3 /root/bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
# Активируйте сервис
systemctl daemon-reload
systemctl enable student-bot
systemctl start student-bot

# Проверьте статус
systemctl status student-bot

# Просмотр логов
journalctl -u student-bot -f
```

---

### 🎯 Вариант 3: Docker (ЛУЧШИЙ СПОСОБ)

Создайте `Dockerfile`:

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

Создайте `docker-compose.yml`:

```yaml
version: '3.8'

services:
  bot:
    build: .
    restart: always
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./student_bot.db:/app/student_bot.db
```

Запустите:

```bash
# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart
```

---

### 🎯 Вариант 4: Бесплатные облачные платформы

#### Railway.app (ПРОСТОЙ СПОСОБ)
```bash
# 1. Зарегистрируйтесь на railway.app
# 2. Нажмите "New Project" → "Deploy from GitHub"
# 3. Выберите ваш репозиторий
# 4. Добавьте переменные окружения (BOT_TOKEN, ADMIN_IDS)
# 5. Deploy!

# ✅ Бесплатно: 500 часов в месяц
```

#### Render.com
```bash
# 1. Зарегистрируйтесь на render.com
# 2. New → Web Service
# 3. Подключите GitHub репозиторий
# 4. Добавьте переменные окружения
# 5. Deploy

# ✅ Бесплатно с ограничениями
```

#### Fly.io
```bash
# 1. Установите flyctl
curl -L https://fly.io/install.sh | sh

# 2. Войдите
flyctl auth login

# 3. Запустите в директории проекта
flyctl launch

# 4. Deploy
flyctl deploy

# ✅ Бесплатно с лимитами
```

---

## 🔧 Рекомендации по выбору:

### Для разработки и тестов:
✅ **Ваш компьютер** - бесплатно, просто

### Для небольших проектов (до 100 пользователей):
✅ **Railway/Render** - бесплатно, просто настроить

### Для среднего бизнеса (100-1000 пользователей):
✅ **VPS (Timeweb/Selectel)** - 200₽/мес, полный контроль

### Для крупного бизнеса (1000+ пользователей):
✅ **VPS с Docker** - масштабируемость, надежность

---

## 📊 Сравнение вариантов:

| Вариант | Цена | Сложность | Надежность | Производительность |
|---------|------|-----------|------------|-------------------|
| Ноутбук | Бесплатно | ⭐ | ❌ | ⭐⭐⭐ |
| Railway | Бесплатно* | ⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| VPS | От 150₽/мес | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Docker на VPS | От 150₽/мес | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

*бесплатно с ограничениями

---

## 🎯 Пошаговый план запуска 24/7:

### Минимальный (бесплатно):
1. Зарегистрируйтесь на Railway.app
2. Подключите GitHub
3. Добавьте переменные окружения
4. Нажмите Deploy
5. ✅ Готово!

### Оптимальный (200₽/мес):
1. Купите VPS на Timeweb
2. Подключитесь по SSH
3. Установите зависимости
4. Настройте systemd service
5. ✅ Бот работает 24/7!

### Профессиональный:
1. VPS + Docker + CI/CD
2. Мониторинг (Grafana)
3. Резервное копирование
4. Load balancer
5. ✅ Production-ready!

---

## 💡 Полезные команды для VPS:

```bash
# Проверить, работает ли бот
ps aux | grep python

# Убить процесс бота
pkill -f main.py

# Просмотр логов в реальном времени
tail -f bot.log

# Перезапуск через systemd
systemctl restart student-bot

# Автоматический перезапуск при падении
# (уже настроено в systemd service выше)

# Обновление бота
git pull
systemctl restart student-bot
```

---

## 🔒 Безопасность на VPS:

```bash
# 1. Смените пароль root
passwd

# 2. Создайте нового пользователя
adduser botuser
usermod -aG sudo botuser

# 3. Настройте firewall
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable

# 4. Отключите вход по паролю (используйте SSH ключи)
nano /etc/ssh/sshd_config
# PasswordAuthentication no
systemctl restart sshd

# 5. Установите fail2ban
apt install fail2ban
systemctl enable fail2ban
```

---

## 📈 Мониторинг бота:

### Простой способ:
```bash
# Создайте скрипт check_bot.sh
#!/bin/bash
if ! pgrep -f "python3 main.py" > /dev/null
then
    echo "Bot is down! Restarting..."
    cd /root/bot && nohup python3 main.py &
fi

# Добавьте в crontab (проверка каждые 5 минут)
crontab -e
*/5 * * * * /root/check_bot.sh
```

### Продвинутый способ:
- **Prometheus** + **Grafana** - метрики
- **Sentry** - отслеживание ошибок
- **Uptime Robot** - проверка доступности

---

## 🎉 ИТОГО:

### Для начала:
👉 **Railway.app** - развернется за 5 минут, бесплатно

### Для бизнеса:
👉 **VPS (Timeweb)** - 200₽/мес, полный контроль

### Когда вырастете:
👉 **Несколько VPS + Load Balancer + Monitoring**

---

## ❓ Частые вопросы:

**Q: Какой VPS выбрать?**
A: Для начала хватит самого дешевого (1 CPU, 1GB RAM)

**Q: Сколько пользователей выдержит?**
A: На базовом VPS - до 10,000 активных пользователей

**Q: Нужна ли база PostgreSQL?**
A: До 1000 пользователей - SQLite достаточно

**Q: Как сделать backup?**
A: Копируйте student_bot.db ежедневно через cron

---

## 📞 Нужна помощь?

Если возникли сложности с развертыванием:
1. Проверьте логи
2. Погуглите ошибку
3. Спросите в сообществе
4. Напишите в поддержку хостинга

---

**Создано для вашего удобства! 🚀**

