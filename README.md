# 🎓 Студенческий Аутсорс Бот

Telegram-бот для продажи готовых учебных проектов и приема индивидуальных заказов на разработку.

## 📋 Возможности

### Для пользователей:
- 🛍 **Каталог готовых проектов** - дипломы, курсовые, презентации, IT-проекты
- 📝 **Индивидуальные заказы** - разработка под ваши требования
- 🛒 **Корзина покупок** - удобное оформление заказов
- 📦 **Мои заказы** - история покупок и отслеживание статуса заказов
- 👤 **Профиль** - персональные данные и статистика
- 💬 **Поддержка** - система тикетов и FAQ

### Для администраторов:
- 📊 **Статистика и аналитика** - метрики пользователей и продаж
- 📚 **Управление каталогом** - добавление, редактирование, удаление проектов
- 📋 **Управление заказами** - обработка индивидуальных заказов
- 📢 **Система рассылки** - таргетированные сообщения пользователям
- 👥 **Управление пользователями** - просмотр, блокировка
- 📁 **Управление категориями** - организация каталога

## 🚀 Установка и запуск

### Требования
- Python 3.10 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Шаг 1: Клонирование репозитория
```bash
git clone <repository_url>
cd bot
```

### Шаг 2: Создание виртуального окружения
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка окружения
Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните необходимые параметры:

```env
# Telegram Bot Token (ОБЯЗАТЕЛЬНО)
BOT_TOKEN=your_bot_token_here

# Database (по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///./student_bot.db

# Admin IDs (telegram user IDs администраторов через запятую)
ADMIN_IDS=123456789,987654321

# Debug режим
DEBUG=True
```

### Шаг 5: Инициализация базы данных
База данных создастся автоматически при первом запуске бота.

### Шаг 6: Запуск бота
```bash
python main.py
```

Бот запущен! ✅

## 📁 Структура проекта

```
bot/
├── bot/
│   ├── database/           # Модели и работа с БД
│   │   ├── models.py      # SQLAlchemy модели
│   │   ├── crud.py        # CRUD операции
│   │   └── engine.py      # Подключение к БД
│   ├── handlers/          # Обработчики команд
│   │   ├── user.py        # Основные команды
│   │   ├── catalog.py     # Каталог проектов
│   │   ├── cart.py        # Корзина
│   │   ├── orders.py      # Заказы
│   │   ├── profile.py     # Профиль
│   │   ├── support.py     # Поддержка
│   │   └── admin.py       # Админ-панель
│   ├── keyboards/         # Клавиатуры
│   │   ├── user.py        # Пользовательские
│   │   └── admin.py       # Админские
│   ├── states/            # FSM состояния
│   │   └── order.py       # Состояния для заказов
│   ├── middlewares/       # Middleware
│   │   └── database.py    # Middleware для БД
│   └── utils/             # Утилиты
│       └── helpers.py     # Вспомогательные функции
├── uploads/               # Загруженные файлы
│   ├── projects/          # Файлы проектов
│   └── orders/            # Файлы заказов
├── config.py              # Конфигурация
├── main.py                # Точка входа
├── requirements.txt       # Зависимости
├── .env.example           # Пример .env файла
├── .gitignore            
└── README.md              # Этот файл
```

## 🔧 Конфигурация

### База данных

#### SQLite (по умолчанию)
Подходит для разработки и небольших проектов:
```env
DATABASE_URL=sqlite+aiosqlite:///./student_bot.db
```

#### PostgreSQL (рекомендуется для продакшена)
Для больших нагрузок используйте PostgreSQL:

1. Установите PostgreSQL
2. Создайте базу данных
3. Раскомментируйте `asyncpg` в `requirements.txt`
4. Обновите `.env`:
```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/student_bot
```

### Администраторы

Добавьте Telegram ID администраторов в `.env`:
```env
ADMIN_IDS=123456789,987654321
```

**Как узнать свой Telegram ID:**
1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Он вышлет ваш ID

### Файлы проектов

Файлы хранятся в директории `uploads/`. Убедитесь, что у приложения есть права на запись.

## 📝 Использование

### Основные команды бота:
- `/start` - Запуск бота и регистрация
- `/menu` - Главное меню
- `/help` - Справка
- `/admin` - Админ-панель (только для администраторов)

### Добавление проектов в каталог

Для добавления проектов используйте админ-панель или напрямую через базу данных.

Пример добавления категории и проекта через Python:

```python
import asyncio
from bot.database import crud, init_db
from bot.database.engine import async_session_maker
from bot.database.models import ProjectType, ProjectLevel

async def add_sample_data():
    await init_db()
    
    async with async_session_maker() as session:
        # Создаем категорию
        category = await crud.create_category(
            session,
            name="Python",
            description="Проекты на Python",
            icon="🐍"
        )
        
        # Создаем проект
        project = await crud.create_project(
            session,
            title="Телеграм-бот для интернет-магазина",
            description="Полнофункциональный бот с корзиной, оплатой и админ-панелью",
            category_id=category.id,
            project_type=ProjectType.PROJECT,
            level=ProjectLevel.ADVANCED,
            technologies="aiogram, SQLAlchemy, PostgreSQL",
            programming_languages="Python",
            price=5000.0,
            is_active=True
        )
        
        print(f"Создан проект: {project.title}")

if __name__ == "__main__":
    asyncio.run(add_sample_data())
```

## 🛠 Разработка

### Добавление новых функций

1. **Новые модели БД**: добавьте в `bot/database/models.py`
2. **CRUD операции**: добавьте в `bot/database/crud.py`
3. **Обработчики**: создайте новый файл в `bot/handlers/`
4. **Клавиатуры**: добавьте в `bot/keyboards/`
5. **FSM состояния**: добавьте в `bot/states/`

### Структура handler'а
```python
from aiogram import Router, F
from aiogram.types import CallbackQuery
from sqlalchemy.ext.asyncio import AsyncSession

router = Router()

@router.callback_query(F.data == "your_callback")
async def your_handler(callback: CallbackQuery, session: AsyncSession):
    # Ваш код
    await callback.answer()
```

Не забудьте зарегистрировать router в `main.py`:
```python
from bot.handlers import your_module
dp.include_router(your_module.router)
```

## 🐛 Решение проблем

### Ошибка "Invalid token"
- Проверьте правильность `BOT_TOKEN` в `.env`
- Убедитесь, что токен получен от [@BotFather](https://t.me/BotFather)

### База данных не создается
- Проверьте права на запись в директории проекта
- Для PostgreSQL убедитесь, что сервер запущен и доступен

### Бот не отвечает
- Проверьте подключение к интернету
- Убедитесь, что токен валиден
- Проверьте логи в консоли

### Админ-панель недоступна
- Проверьте, что ваш Telegram ID добавлен в `ADMIN_IDS`
- Используйте команду `/admin` для доступа

## 📊 Мониторинг и логи

Бот логирует все важные события. Для просмотра:
- Логи выводятся в консоль
- Уровень логирования настраивается через `DEBUG` в `.env`

## 🔒 Безопасность

- Никогда не публикуйте `.env` файл
- Храните `BOT_TOKEN` в секрете
- Регулярно делайте backup базы данных
- Используйте сильные пароли для PostgreSQL

## 📈 Масштабирование

Для больших нагрузок:
1. Используйте PostgreSQL вместо SQLite
2. Добавьте Redis для кеширования
3. Используйте webhook вместо polling
4. Разверните на выделенном сервере (VPS)

## 🤝 Вклад в проект

Нашли баг или хотите добавить функцию? Создайте issue или pull request!

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 📞 Поддержка

Если у вас возникли вопросы:
- Изучите FAQ в боте
- Проверьте раздел "Решение проблем" выше
- Создайте issue на GitHub

---

**Разработано с ❤️ для студентов**

